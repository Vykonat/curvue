type Comment {
  id: ID!
  content: String!
  commentable_id: Int!
  commentable_type: String!
  user: User! @belongsTo
  likes: [Like!] @hasMany
  replies: [Comment!] @hasMany(relation: "replies")
  created_at: DateTime!
  updated_at: DateTime!
}
